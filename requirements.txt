# Core GUI
PySide6>=6.6,<7.0

# Speech-to-text (Whisper)
openai-whisper>=20231117

# Speaker diarization (Hugging Face)
pyannote.audio==4.0.2
huggingface_hub>=0.36.0,<1.0

# PyTorch stack (required by pyannote)
# If you have installation issues, install torch/torchaudio manually first (CPU/CUDA).
torch>=2.2
torchaudio>=2.2

# Common utilities (often installed transitively, listed for stability)
numpy>=1.26
tqdm>=4.66

# GUI
PySide6>=6.5

# Audio I/O
soundfile>=0.12.1

# ML core
torch>=2.1.0
torchaudio>=2.1.0

# Utilities
numpy>=1.24
